<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="RedlinePanel" TargetType="Border">
    <Setter Property="CornerRadius" Value="8" />
    <Setter Property="BorderBrush" Value="#34383D" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="16" />
    <Setter Property="Background">
      <Setter.Value><LinearGradientBrush StartPoint="0,0" EndPoint="1,1"><GradientStop Color="#14171A" /><GradientStop Color="#08090B" Offset="1" /></LinearGradientBrush></Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="RedlineGlyph" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
    <Setter Property="FontSize" Value="20" />
    <Setter Property="Width" Value="30" />
    <Setter Property="VerticalAlignment" Value="Center" />
  </Style>
  <Style x:Key="RedlineToggle" TargetType="CheckBox">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Border x:Name="Track" CornerRadius="18" Width="80" Height="36" Background="#191D22" BorderBrush="#7B858E" BorderThickness="1">
            <Grid Margin="5"><Ellipse x:Name="Thumb" Width="24" Height="24" Fill="#CFD3D8" HorizontalAlignment="Left" /><TextBlock x:Name="Label" Text="Off" Foreground="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" /></Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True"><Setter TargetName="Track" Property="Background" Value="#A40020" /><Setter TargetName="Track" Property="BorderBrush" Value="#FF2945" /><Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" /><Setter TargetName="Thumb" Property="Fill" Value="White" /><Setter TargetName="Label" Property="Text" Value="On" /><Setter TargetName="Label" Property="HorizontalAlignment" Value="Left" /><Setter TargetName="Label" Property="Margin" Value="5,0,0,0" /></Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Track" Property="BorderBrush" Value="White" /><Setter TargetName="Track" Property="BorderThickness" Value="2" /></Trigger>
            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Track" Property="Opacity" Value="0.5" /></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
